// let config = require('../config.json');

// module.exports.execute = async (client, message) => {
// 	let newMessage;
// 	if (message.channel == config.channels.raidroom50) {
// 		newMessage = '**RAAAAAAAAAAAAAAAAAAAID⚔** with ' + `<@${message.author.id}>` + '\nhttps://cuckoo.team/raid50';
// 	}
// 	else if (message.channel == config.channels.raidroom90) {
// 		newMessage = '**RAAAAAAAAAAAAAAAAAAAID⚔** with ' + `<@${message.author.id}>` + '\nhttps://cuckoo.team/raid90';
// 	}
// 	else {
// 		newMessage = '**RAAAAAAAAAAAAAAAAAAAID⚔** with ' + `<@${message.author.id}>` + '\nhttps://cuckoo.team/koa';
// 	}
// 	return await message.channel.send(newMessage);
// };

// module.exports.config = {
// 	name: 'raid',
// 	aliases: ['raid' ,'r'],
// 	description: 'RAAAAAAAAAID!',
// 	usage: ['raid'],
// };
